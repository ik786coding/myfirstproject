/*const coffeeOrder = (size, drinkType) => {
    console.log(`coffee size ${size} drink type ${drinkType}`)

}
coffeeOrder("large", "hotChocolate");
coffeeOrder("small", "cappuccino");
coffeeOrder("large", "coffee");*/

/*let orderCount = 0;
const takeOrder = (topping, topping2, topping3) => {
 console.log(`Pizza with ${topping} ${topping2} ${topping3}`);
 orderCount++;
 console.log(orderCount)
}
takeOrder("pineapple","chicken","sweetcorn");
takeOrder("Donner","onions","pepper");*/

/*|let balance = 500
let pin = 5045
const cashWithdrawal = (amount, accnum) => {
    if (pin == 5044 && balance >= amount){
        console.log(`pin correct and withdrawing cash from ${accnum}`); 
    }
 else if (amount > balance && pin == 5044){
     console.log("insufficent funds")
    }
else if (balance >= amount && pin !=5044){
    console.log("incorrect pin")
    }
else if (balance < amount && pin !=504){
    console.log("insuicient funds and incorrect pin")
}}*/

 
 //else {
            //console.log("wrong pin")
        //}
    //console.log(`withdfrawing ${amount} from account ${accnum}`)

//cashWithdrawal(300, 50449921);
//cashWithdrawal(700, 50449921);
//cashWithdrawal(200, 50447921);

let subSandwich = 0;
const order = (filling1, filling2, filling3, filling4, filling5) => {
 console.log(`sandwich with ${filling1} ${filling2} ${filling3} ${filling4} ${filling5}`);
 subSandwich++;
 console.log(subSandwich)
    
}
order("Tuna","sweetcorn","onions","peppers","Mayonaise");

